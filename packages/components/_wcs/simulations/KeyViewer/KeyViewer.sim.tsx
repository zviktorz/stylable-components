
import { createSimulation } from '@wixc3/react-simulation';
import { KeyViewer } from '../../../src/key-viewer/key-viewer';

export default createSimulation({
    name: 'KeyViewer',
    componentType: KeyViewer,
    props: {}
});
